[global]
log_level = 'debug'

[mode]

[mode.clients]
enabled = true
refresh = true
misbehaviour = true

[mode.connections]
enabled = false

[mode.channels]
enabled = false

[mode.packets]
enabled = true
clear_interval = 10
clear_on_start = false
tx_confirmation = true

[telemetry]
enabled = false
host = '127.0.0.1'
port = 3001

[[chains]]
id = 'local.d3c75ccd0e6f04b1a164a67e'  # set your chain ID #TODO: update this using script
type = 'Namada'
rpc_addr = 'http://namada-1:26657'  # set the IP and the port of the chain
grpc_addr = 'http://namada-1:9090'  # not used for now
event_source = { mode = 'push', url = 'ws://namada-1:26657/websocket', batch_delay = '500ms' }  # set the IP and the port of the chain
account_prefix = ''  # not used
key_name = 'relayer'  # The key is an account name you made
store_prefix = 'ibc'
gas_price = { price = 0.001, denom = 'tnam1q87wtaqqtlwkw927gaff34hgda36huk0kgry692a' }  #TODO: update this using script # the price isn't used for now, the denom should be a raw token address
trusting_period = '1333s' #TODO : use it in mainnet
#trusting_period = '1s'
rpc_timeout = '60s'

[[chains]]
id = 'local.ff672a091f835e7c33ee8b8a' #TODO: update this using script
type = 'Namada'
rpc_addr = 'http://namada-chain-b:26657'
grpc_addr = 'http://namada-chain-b:9090'
event_source = { mode = 'push', url = 'ws://namada-chain-b:26657/websocket', batch_delay = '500ms' }
account_prefix = ''
key_name = 'relayer'
store_prefix = 'ibc'
gas_price = { price = 0.001, denom = 'tnam1q87wtaqqtlwkw927gaff34hgda36huk0kgry692a' } #TODO: update this using script
trusting_period = '1333s' #TODO : use it in mainnet
#trusting_period = '1s'
rpc_timeout = '60s'